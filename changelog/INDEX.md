# Changelog 索引

> **范围**：功能变更（新功能 / 行为修改 / API / 依赖升级）。Debug / 性能 / 安全 review 见 [`reviews/`](../reviews/INDEX.md)（CHANGELOG_6 起划分）。

| 文件 | 概要（≤80 字） |
|------|------|
| [CHANGELOG_1.md](CHANGELOG_1.md) | 新增 Profile 系统：CLI + UI 双入口，env / symlink 双切换模式，pre/post Hook |
| [CHANGELOG_2.md](CHANGELOG_2.md) | 修复 env 模式 user-level settings.json env 泄漏：env 模式补做 symlink swap |
| [CHANGELOG_3.md](CHANGELOG_3.md) | 移除 env 切换模式（含 spawnTerminal / TerminalApp / --mode / --terminal），统一只走 symlink |
| [CHANGELOG_4.md](CHANGELOG_4.md) | 新增 `dch profile env` + ~/.zshrc 子 shell wrapper，让 profile.env 落到 claude / codex 进程（OAuth / API 走代理） |
| [CHANGELOG_5.md](CHANGELOG_5.md) | 修 UI 删除 profile 卡死（Tauri 2 不弹 window.confirm）改内联确认；新建表单加 preHook/postHook + 模型配置（settings.json/config.toml）一次填齐 |
| [CHANGELOG_6.md](CHANGELOG_6.md) | 跨平台兼容性 Windows 支持（REVIEW_1 落地）：新增 platform.ts 抽象 + cli/store path 修复 + symlink → junction 平台分流 + hooks 协议加 PowerShell/cmd（向后兼容 string）+ Tauri Rust 全套 cfg(target_os) 守门 + readers Win 平台分流；38 bun test 全过 mac 端零回退；Win 端真机 E2E 留 CI |
| [CHANGELOG_7.md](CHANGELOG_7.md) | REVIEW_2 落地（综合 deep review fix，按 PR 追加）：PR-1 测试地基 — store loadStore/saveStore 加可选 path + 9 case loadStore 边界 + parseFlags export + 14 case 回归保护 + 6 case pathState 四态；38 → 68 pass + 1 skip（H3 待 PR-5） |
| [CHANGELOG_8.md](CHANGELOG_8.md) | Schema-driven 精细化配置 + CM6 + Markdown 渲染（**10 PR 全合 + 4 follow-up + REVIEW_3 + REVIEW_4 收口**）：PR-A 类型骨架 / PR-B JSON 写回保留 / PR-F CM6 替换 raw + dotfile/markdown view / REVIEW_3 双异构对抗 23 fix（5H/9M/9L+3 AP）/ PR-C 14 个 fields 控件 / PR-D 接入 Claude settings.json schema 行内编辑（**用户首波感知** + CLAUDE.md Schema 三铁律 + README）/ PR-E 扩展到 Codex/OpenCode/.mcp.json + toml-patcher 11 case / PR-G CM6 edit + JSON Schema lint+hover+completion + TOCTOU 完整 banner / PR-H Markdown 渲染（react-markdown + shiki lazy + GFM + sanitize）/ PR-I ProfilePanel 拆 7 文件（789→261）+ dch-store schema + ProfileStoreEditor / PR-J sync.ts 自动化（list/--check-self/--fetch/--list-scopes）+ ajv runtime 校验 + bundle splitting (4.76→3.65 MB entry + 11 chunks) + 文档收口 / **Follow-up**: #3 字段 errors Context 按 path 分发 / #4 GitHub Action schema-sync cron / #1 PathField Tauri dialog / #2 happy-dom CMEditor + MarkdownView 单测 / **REVIEW_4 双异构对抗 2 轮 30 fix（5H/16M/9L+3 AP）**：H1 onConflictReload TOML 硬编 JSON.parse / H1' defaultMode enum 漏 4 项 / H2 dch-store profile.id pattern / R-H1 process.env.HOME bun bundler inline / R-H2 严校验比上游严回归回退 / R-M3 saving 期间 reload 覆盖 in-flight；**76 → 209 pass / 0 回归** |
| [CHANGELOG_9.md](CHANGELOG_9.md) | 自定义 schema 本地 override（`~/.dch/schemas/<scopeKind>.json` 字段级合并 — `mergeSchemas` shallow merge / propertyOrder override 优先 / additionalProperties 强制 true / $id+scopeKind 不允许 override）+ 字段隐藏（root level「⋯」popover 菜单 + ui-prefs.json 持久化 + advanced 默认隐藏 + 顶部「显示隐藏字段 (N)」toggle + 「取消隐藏」反向 toggle 按钮）+ schema 字段补全（Claude 12 + Codex 15 + OpenCode 5 个常见字段；KV 化 hooks/pluginConfigs/extraKnownMarketplaces/enabledPlugins/mcp_servers/opencode provider/agent/tools/command/mcp）+ ebnf CJS interop bun patch 持久化 + 嵌套 KV 布局 vertical stack 修错位；REVIEW_5 系列 hot-fix 完整收口（卡 Loading / [object Object] / enabledPlugins 误判 / KV value 列错位 / 字段隐藏还原）；**209 → 218 pass / 0 回归** |
| [CHANGELOG_10.md](CHANGELOG_10.md) | 工具配置 / Profile 自动刷新（focus + visibilitychange + 5s mtime poll）+ REVIEW_6 deep review 8 fix 收口（2 HIGH + 5 MED + 1 LOW，3 轮异构对抗 + 2 反驳轮）+ 12 个新 component test（防未来 PR 静默回退）；218 → 230 pass / 0 fail / 0 回归 |
| [CHANGELOG_11.md](CHANGELOG_11.md) | tab 切换卡顿修复：panel 常驻 + display 切换（消除 ConfigPanel/ProfilePanel mount 重做 N 次 IPC + dch CLI spawn + 字段树重建 + shiki 重渲染）+ visibility-aware 5s poll（隐藏 panel 暂停 12-16 个 timer）；230 pass / 0 回归 |
| [CHANGELOG_12.md](CHANGELOG_12.md) | 切 profile 卡死全链路修复（REVIEW_7 双异构对抗 7H+3M+1L）：CLI flushAndExit + jsonOut async write callback 防 macOS 65536 byte stdout 截断 + runProfileCommand 末尾强退；Rust 抽 proc_timeout::spawn_with_timeout helper（process group setsid + 增量收 + killpg SIGKILL 杀整组 + 不 await reader join）+ get_tool_version 同源修；UI 按命令传 timeout（use=2×hookTimeout+5s）；3 bun e2e + 5 cargo test；233 pass + 5 cargo / 0 回归 |
| [CHANGELOG_13.md](CHANGELOG_13.md) | 切到 Profile tab 卡顿修复：IPC 风暴去重 + ProfilePanel 受控化。外部切回 Tauri 窗口同时 fire focus + visibilitychange × ProfilePanel/App 各一对 listener = 14 IPC 砸主线程。修：profile 数据上提 App.tsx 单点 + 100ms 窗口 dedupe + reloading guard；ProfilePanel 改受控接 store/active props 删自己 listener；CRUD 不再反调 App.load()。IPC 14 → 7 并发；233 pass / 0 回归 |
| [CHANGELOG_14.md](CHANGELOG_14.md) | 删 schema 系统 + 列表模式 + 新建表单简化 + 自定义下拉。删 src/schemas/ 多余 19 文件 + schema-mode/ + fields/ 整目录 + descriptions.ts；ConfigPanel 简化为 view/edit/markdown render 三模式；AddProfileModal 删 model/reasoning 字段，主配置文件走 textarea 一把梭；新增 Select.tsx 替代原生下拉框深色主题；保留 dch-store schema + schema-lint 仅服务 ProfileStoreEditor。CSS 450 → 278 行；114 pass / 0 fail |
| [CHANGELOG_15.md](CHANGELOG_15.md) | 切回窗口卡顿修复（focus reload 零进程 spawn）：versions 缓存到首屏 + profile data 直读 fs 替代 dch list/current spawn + readScope 改 readFileWithMtime 减半 IPC + 抽 store-shape.ts 共享 default 补全（前端 / CLI 同源防分叉）+ 新增 Rust read_link IPC + 首屏 reloadingRef guard 防 race；切回开销 6 spawn+21 IPC → 0 spawn+11 IPC；114 → 132 pass + 5 → 10 cargo / 0 回归 |
| [CHANGELOG_16.md](CHANGELOG_16.md) | 备份/还原（.dchpack 单文件归档）：`dch profile backup` / `restore` + UI 双按钮，全部 profile + `~/.dch/scripts/` + `~/.agents/` 打包；默认脱敏 token / API key 为 `<<DCH_PLACEHOLDER:KEY>>`（路径类后缀豁免）；撞名自动加 `-restored-<TS>` 后缀；`--no-placeholder` 旁路含明文凭据需二次确认；`--dry-run` 看冲突 + 占位符 + 共享 diff plan；端到端跑通：41.6MB / 7794 entry / 73 处脱敏 / restore --prefix 真还原 + 清理验证。49 个新单测全过 |
| [CHANGELOG_17.md](CHANGELOG_17.md) | 备份三层模型（默认位 + 置顶 + 历史）+ UI 备份历史 modal：`dch profile backup` 默认覆盖 `~/.dch/backups/latest.dchpack`，`--keep` 写时间戳历史副本；`backup-pin` 加 sidecar 永不被覆盖（默认位 → 复制副本 + 置顶）；新增 `backups / backup-rm / backup-pin` 3 个 CLI + UI `📚 备份历史` modal 三区列表 + 还原 / 置顶 / 删除按钮；同步抽 cli-shared.ts + cli-backup.ts 解决 cli-profile 588 行超 500 护栏（→ 341 行）；195 pass / 0 回归 |
| [CHANGELOG_18.md](CHANGELOG_18.md) | 备份敏感字段去重 + 还原阶段交互式补值：manifest 加 `secrets_index`（实测 148 处 → 32 logical key 4.6x 压缩）；CLI `restore --fill-secrets`（隐藏输入）/ `--secrets-json <file>`（自动化）；UI RestoreBackupModal 加 step 3「填 K 个 secret」+ 4 状态 banner / eye toggle / skip checkbox；Tauri Rust tempfile 全包 secret 生命周期（drop guard + 0o600）；旧 dchpack 无 `secrets_index` 自动 fall back；195 → 243 pass / 0 回归 |
