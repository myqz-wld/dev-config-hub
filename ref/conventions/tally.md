# Project Convention Candidates (Pending Observation)

> Maintained automatically by agents. **Do not delete entries manually**.
> For the workflow, see the root `CLAUDE.md` section "Repeated Feedback / Repeated Pitfalls -> Promote Conventions".
> When count >= 3, run the three-state verdict review, then **create** `ref/conventions/<X>-<topic>.md` (incrementing X), sync `ref/conventions/INDEX.md`, and remove that entry from this tally.

---

# User Feedback Candidates

Sort by `count` descending.

| ID | Description | count | first_at | last_at | Trigger example |
|----|------|-------|----------|---------|----------|

---

# Agent Pitfall Candidates

Sort by `count` descending.

| ID | Description | count | first_at | last_at | Trigger example |
|----|------|-------|----------|---------|----------|
| AP-1 | `??` vs `\|\|` semantics in `String.trim()` fallback cases: `trim()` always returns a string, not nullish, so `?? "fallback"` never fires. | 1 | 2026-05-04 | 2026-05-04 | REVIEW_1 M1: bridge.ts:135 `parsed.error ?? r.stderr.trim() ?? \`exit ${r.code}\`` made the UI Error toast display an empty string. |
| AP-2 | UI fire-and-forget async plus synchronous setState to close the edit view creates a data-loss risk: when save fails after the textarea is unmounted, `buf` remains in state but the user cannot reach it; clicking edit again runs `setBuf(scope.content)` and overwrites it. | 1 | 2026-05-04 | 2026-05-04 | REVIEW_1 H2: ConfigPanel.tsx:70 `onClick={() => { onSave(...); setMode("view"); }}` |
| AP-3 | A read-modify-write three-step sequence without cross-process locking causes lost updates. Any `loadStore -> mutate -> saveStore` pattern needs a file lock / CAS / single-writer consolidation. | 1 | 2026-05-04 | 2026-05-04 | REVIEW_1 H3: profiles/store.ts manager has 7 write operations; spawning 5 child pushes reproduced losing 4. |
| AP-4 | `Bun.spawn` `proc.kill()` uses SIGTERM by default, which `trap "" TERM` can ignore; if a hook script detaches a child that holds the stdout pipe, `new Response(proc.stdout).text()` never resolves, so timeout exists in name only. | 1 | 2026-05-04 | 2026-05-04 | REVIEW_1 H1: profiles/hooks.ts:39-54; reproduced timeout=5s actually hanging for 10010ms. |
| AP-5 | assert / guard / catch paths must have empirical tests with a real trigger scenario. An unreachable assert is suspect dead code: either delete it or explicitly add `// known dead under <dep>@<version>, kept as regression net`. | 1 | 2026-05-06 | 2026-05-06 | REVIEW_3 R_2 D1: `assertValidJsonOut` has no empirically reachable trigger under jsonc-parser@3.3.1 because all silent-corruption candidates are caught by the outer try/catch, but the comment and test name implied it prevents silent corruption. |
| AP-6 | When passing references across boundaries (`useEffect` / `useMemo` / `Compartment`, etc.), either pass them through directly or short-circuit deps. Forbid in-component `[...arr]` spreading that creates a new ref and invalidates caller-stabilized `useMemo`; otherwise reconfigure / re-render triggers noop transactions. | 1 | 2026-05-06 | 2026-05-06 | REVIEW_3 R_2 D3: CMEditor.tsx `extraCompartment.current.of([...extraExtensions])` triggers noop reconfigure even when the caller has stabilized the value with useMemo. |
| AP-7 | OS syscall Err paths and normal empty values must not be merged into None. Whenever an `.ok().and_then(...).map(...)` chain collapses multiple Err sources, distinguish the semantics: at minimum leave a stderr trace, or use a separate field (`mtime_error`) or sentinel value (`Some(0)`). | 1 | 2026-05-06 | 2026-05-06 | REVIEW_3 R_2 D2: lib.rs read_file_with_mtime merged three None sources, metadata.modified() Err / duration_since(UNIX_EPOCH) Err / file missing, so the PR-D consumer could not distinguish them. |
| AP-8 | Disable `process.env.*` in frontend / Tauri webviews: (1) webviews have no global `process`, so runtime throws; (2) the Bun bundler may inline `process.env.X` by default, writing development-machine paths / secrets directly into the bundle shipped to users, causing wrong paths and information leaks. Always fetch asynchronously through IPC, such as Tauri `getHomeDir()`, even if it costs one async trip. | 1 | 2026-05-06 | 2026-05-06 | REVIEW_4 R_2 R-H1: PathField.tsx previously used `process.env.HOME` to expand and fold `~/`; after Bun bundler inlining, the development-machine path was written into the bundle. |
| AP-9 | "schema / type validation stricter than upstream" is a regression: real valid values become red / rejected in the UI. Before adding pattern / enum / `additionalProperties: false` that is stricter than upstream, WebFetch the upstream schema and verify upstream truly forbids it. Hint-style validation, such as keyPattern only for a UI red box plus CLI gate, must not make ajv hard-reject. | 1 | 2026-05-06 | 2026-05-06 | REVIEW_4 R_2 R-H2: to-json-schema.ts kv-map case previously used `additionalProperties: keyPattern ? false : valueSchema`, causing legal lowercase env (`http_proxy`) to be rejected by ajv, inconsistent with upstream `patternProperties + additionalProperties: { type: string }`. |
| AP-10 | React 19 removed console warnings for setState after unmount during async work, so it is easier to fail silently. Whenever a component may unmount between `await asyncCall(); setState(...)`, such as dialog / fetch / IPC, guard explicitly with `mountedRef.current` / cleanup flag, or it can trigger phantom onChange / silent disk writes. | 1 | 2026-05-06 | 2026-05-06 | REVIEW_4 R_2 R-M1: PathField.tsx onPick could unmount during `await openDialog(...)`, causing phantom onChange disk writes. |
| AP-11 | A boolean state guard in useEffect deps makes the effect rerun with stale props during state transitions. Fix pattern: mirror the boolean guard with `useRef` plus a no-deps useEffect for sync; keep only true business inputs in effect deps; manage async internal state with an independent ref plus explicit try/finally. | 1 | 2026-05-11 | 2026-05-11 | REVIEW_6 hit the same root cause 3 times: R_1-MED-double (saving in deps) + R_2-R1-residual (writingRef not based on saving) + R_2-N-conflict-lost (conflictRef mirror). |
| AP-12 | After fixing one finding, the fix itself often introduces a new finding: R_1 had 4 fixes, then R_2 found 4 more fixes, including an insufficient H1 fix that induced H1-followup HIGH. `Banner displayed != behavior constrained` is a typical UX-fix blind spot: adding a banner is not real blocking; wire the banner state into the underlying disabled / gate expression. Always run Round 2, even if Round 1 fixes look complete. | 1 | 2026-05-11 | 2026-05-11 | REVIEW_6 R_2-H1-followup: ConfigPanel.tsx save button `disabled={saving}` did not include externalChanged; the banner was decorative, and data loss was guaranteed in 6 steps. |
| AP-13 | window/document event listener fixes are normally safe in React 18+ when cleanup correctly calls removeEventListener, closure references match, and useCallback deps are stable; Strict Mode double mount leaves no residue. Reuse this pattern directly; do not suspect a leak every time. Counterexample: repeatedly using `listener leak?` as review focus wastes tokens. | 1 | 2026-05-11 | 2026-05-11 | REVIEW_6 R_1: focus + visibilitychange listeners in App.tsx + ProfilePanel.tsx were checked by both reviewers; cleanup was correct, and Strict Mode validation showed no residue. |
| AP-14 | Bun stdout=pipe plus `process.exit(0)` always truncates >= 65537 bytes to 65536, the macOS pipe buffer limit. Neither `process.stdout.end(cb)` nor `'drain' event` guarantees flush; **only `process.stdout.write(data, callback)` works**. Any pattern that synchronously writes large data then exits must wrap it as `await new Promise(r => process.stdout.write(d, r)); process.exit(c);`. | 1 | 2026-05-12 | 2026-05-12 | REVIEW_7 H1: cli-profile.ts:17 jsonOut + process.exit; both reviewers reproduced >=65537 bytes always truncated to 65536, causing UI JSON.parse to throw SyntaxError; among 3 compared tricks only write+callback worked. |
| AP-15 | The losing setTimeout in `Promise.race([drain, setTimeout(...)])` still keeps the Bun event loop alive. Even after the function returns and the main flow ends, the bun process is dragged until all losing timers fire. Fix: after race, explicitly `clearTimeout` the losing timer; or use `process.exit(0)` as a fallback at main end, which is safe for one-shot CLI. | 1 | 2026-05-12 | 2026-05-12 | REVIEW_7 H7: profiles/hooks.ts:80-95; reproduced hook=`echo ok` runHook function returning in 3ms vs bun process exiting in 4510ms, about timeoutMs+GRACE*2. |
| AP-16 | `Child::kill()`, including `Bun.spawn(...).kill()` / Rust `std::process::Child::kill()`, **kills only the direct child**, not the process tree. Any path that spawns user scripts, such as hook / shell wrapper / source rc, must create a new process group with `setsid` and call `killpg(pgid, SIGKILL)` on timeout to kill the whole tree. Detached grandchildren / async reporting / background processes from sourced rc files can hold parent stdio pipe FDs and hang the reader forever. | 1 | 2026-05-12 | 2026-05-12 | REVIEW_7 H4: lib.rs:321-328 child.kill; ps showed the grandchild sleep 30 still alive and adopted by init after SIGKILL killed the parent sh; fix: libc::setsid + libc::killpg(pgid, SIGKILL). |
| AP-17 | `Bun.file(p).exists()` returns false for directories, even if the directory exists, because BunFile is a "file" abstraction. Any directory-existence check must use `node:fs/promises.stat(p)` + `s.isDirectory()`, where a missing path throws and the catch returns false. Counterexample: treating `Bun.file().exists()` as a universal fs existence check misclassifies directory paths as missing, so later mkdir / copy / readdir are skipped and callers see "success" even though fs did not change; only end-to-end smoke exposes it. | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_16: backup.ts applyBackup used `Bun.file(srcConfigDir).exists()` to check the temporary extraction directory, silently skipped copyDirRecursive, and reported restore "success" even though finalDirAbs did not exist; end-to-end ls exposed it. |
| AP-18 | When adding props to a React function component, the **type declaration** and **parameter destructure** must be updated together. Updating only the type declaration but not the destructure does not produce a TS compile error, because a missing destructure field is not a type error, but runtime `useState(!!myNewProp)` references an undeclared variable, causing ReferenceError and preventing the whole modal / panel from opening. bun build only transforms and does not run strict TS check; tests miss it if they do not mount the component. Defense: edit both places at once, type declaration plus destructure, when adding a prop; every new modal needs a minimal component test, mount plus basic interaction, covering the default prop path. | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_17: ExportBackupModal added presetKeep prop only to the type and not the destructure, so React render threw "Can't find variable: presetKeep" and the export backup modal opened from the package button failed immediately. |
| AP-19 | Tauri v2 `#[tauri::command]` **must be async**, and any >= 100ms work inside must go through `tauri::async_runtime::spawn_blocking`. Non-async commands execute on the webview **main thread**, freezing the React render loop plus all setInterval / setTimeout / requestAnimationFrame. Symptoms: after the user clicks, the UI appears completely frozen, the progress bar does not move, the spinner does not spin, and the elapsed timer does not tick; final state renders all at once only after the command returns. Any command that invokes external commands / spawns processes / does sync fs large-file IO must be async. Microsecond-level single-file read/write can be ignored. Counterexample: `fn read_file(path) -> Result<String, String>` is fine for short fs ops; `fn run_dch_command(args, timeout) -> Result<...>` spawning bun plus a try_wait loop blocks 5-10s and is disastrous. This also explains why adding BackupProgress rendering did not help and why backup history freezes for 500ms each time; the issue is unrelated to React code. | 1 | 2026-05-13 | 2026-05-13 | CHANGELOG_17 follow-up fix, commit 9dd5d0b: codex rescue empirically showed lib.rs:277 fn run_dch_command blocking the main thread, so after the user clicked the export backup package button it took 5-10s to show any result while BackupProgress and setInterval(elapsed) were frozen; changing it to async fn + spawn_blocking fixed it. |
| AP-20 | Changing `process.env.HOME` in tests does not affect module constant caches, because Node.js `os.homedir()` reads the passwd db once at module load. Any code that depends on import-time computed constants such as `import { HOME, STORE_PATH } from '...platform.ts/store.ts'`, including saveStore / loadStore / writeFile(STORE_PATH), must be isolated through a spawned child process plus `env: { HOME: tmpDir }`; otherwise in-process tests write directly into real ~/.dch/profiles.json. Counterexample: in a test, `process.env.HOME = tmp; await saveStore(s, STORE_PATH)` leaves STORE_PATH pointing to the real home path and instantly overwrites user config. Defense: all fs-side-effect tests use `Bun.spawn(['bun', CLI_PATH, ...args], { env: { ...process.env, HOME: tmp }})` pattern, see cli-profile.exit-time.test.ts / manager.stale-lock.test.ts / backup-safety.test.ts. | 1 | 2026-05-14 | 2026-05-14 | REVIEW_8 D7 implementation pitfall: backup-safety.test.ts initially changed process.env.HOME in process and ran saveStore + createBackup, causing ~/.dch/profiles.json to be overwritten and ~/.dch-restored/src to be created; after emergency restore from latest.dchpack, the test changed to spawned child process mode. |
